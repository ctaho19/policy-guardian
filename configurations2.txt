# First, let's see what columns we actually have
print("Column names in unmapped_validation_with_status:")
unmapped_validation_with_status.columns

# Then, select the columns using the exact names
additional_mapped_roles = unmapped_validation_with_status.select(
    unmapped_validation_with_status.columns[0].alias("Resource_ID"),  # Select first Resource_ID column
    col("ROLE_TYPE").alias("Role_Type")
)

# Alternatively, if you know which one you want:
additional_mapped_roles = unmapped_validation_with_status.select(
    col("RESOURCE_ID").alias("Resource_ID"),  # Note: using exact case as shown in error
    col("ROLE_TYPE").alias("Role_Type")
)
